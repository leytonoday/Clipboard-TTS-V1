const state = {
    theme: {name: "dark", background: "#262626", text: "white", accent: "#3AC47C"}, // current theme
}

const mutations = {
}

const actions = {
}

const getters = {
    theme: state => state.theme,
}

export default { state, mutations, actions, getters }